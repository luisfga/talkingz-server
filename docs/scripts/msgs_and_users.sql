SELECT msg.id, msg.msg_status, msg.content, msg.dest_id, msg.sender_id, au.user_name, msg.sent_time, msg.mime_type, msg.media_thumbnail
	FROM public.message as msg inner join public.app_user as au ON msg.sender_id = au.id;